name: dv_gravures_mobile
description: Mobiele DV Gravures app voor iPhone/iPad en Android.
publish_to: "none"
version: 0.2.1+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_secure_storage: ^10.3.1
  image_picker: ^1.2.3
  camera: ^0.12.0+2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
